import { InputCurrencyDirective } from './input-currency.directive';

describe('InputCurrencyDirective', () => {
  it('should create an instance', () => {
    const directive = new InputCurrencyDirective();
    expect(directive).toBeTruthy();
  });
});
