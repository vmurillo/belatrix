import { InputformatPipe } from './inputformat.pipe';

describe('InputformatPipe', () => {
  it('create an instance', () => {
    const pipe = new InputformatPipe();
    expect(pipe).toBeTruthy();
  });
});
